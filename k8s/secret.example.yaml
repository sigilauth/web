apiVersion: v1
kind: Secret
metadata:
  name: sigil-web-env
  namespace: sigil
type: Opaque
stringData:
  # SvelteKit runtime environment variables
  # Replace placeholder values before applying
  PUBLIC_API_BASE: "https://api.sigilauth.com"
  ORIGIN: "https://sigilauth.com"
  # Add other PUBLIC_ env vars as needed per SvelteKit convention
